# Core Web & API Dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.12
pillow==10.4.0

# LangChain & Mistral AI (mistral-medium-latest)
langchain==0.3.7
langgraph==0.2.45
langchain-mistralai==0.2.1

# Real OCR pipeline (used when Demo Mode is off).
# Tesseract via pytesseract, not PaddleOCR - see ocr_service.py's module
# docstring for why. pytesseract is a thin wrapper around the `tesseract`
# system binary (installed in apps/api/Dockerfile, not pip-installable) -
# no deep-learning framework, no model downloads, far lighter than
# PaddleOCR/paddlepaddle was. Image decoding/resizing uses pillow (already
# a dependency above), so opencv/numpy are no longer needed either.
pytesseract==0.3.13

# Disabled / Optional Integrations (commented out for lightweight testing)
# psycopg2-binary==2.9.9
# langsmith==0.1.140
